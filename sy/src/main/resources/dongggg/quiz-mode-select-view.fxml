<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.*?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="dongggg.QuizModeSelectController"
            stylesheets="@styles.css"
            styleClass="root-pane, main-root">

    <center>
        <HBox alignment="CENTER" spacing="32">
            <padding>
                <Insets top="40" right="32" bottom="40" left="32"/>
            </padding>

            <!-- 전체 시험 보기 카드 -->
            <VBox spacing="18"
                  alignment="TOP_CENTER"
                  prefWidth="420"
                  styleClass="quiz-mode-card purple">
                <VBox alignment="CENTER" spacing="10">
                    <Label text="📖" styleClass="quiz-mode-emoji, quiz-mode-icon-purple"/>
                    <Label text="전체 시험 보기" styleClass="quiz-mode-title"/>
                    <Label text="모든 문제를 풀어보세요" styleClass="quiz-mode-subtitle"/>
                </VBox>

                <VBox spacing="8" styleClass="quiz-mode-info">
                    <HBox alignment="CENTER_LEFT" spacing="8">
                        <Label text="총 문제 수" styleClass="quiz-mode-info-label"/>
                        <Label fx:id="totalCountLabel" text="0문제" styleClass="quiz-mode-info-value"/>
                    </HBox>
                    <HBox alignment="CENTER_LEFT" spacing="8">
                        <Label text="예상 소요시간" styleClass="quiz-mode-info-label"/>
                        <Label fx:id="totalTimeLabel" text="0분" styleClass="quiz-mode-info-value"/>
                    </HBox>
                </VBox>

                <Button text="시작하기"
                        styleClass="quiz-mode-start purple"
                        onAction="#onStartAll"/>
            </VBox>

            <!-- 오답률 상위 시험 카드 -->
            <VBox spacing="18"
                  alignment="TOP_CENTER"
                  prefWidth="420"
                  styleClass="quiz-mode-card yellow">
                <VBox alignment="CENTER" spacing="10">
                    <Label text="🎯" styleClass="quiz-mode-emoji, quiz-mode-icon-yellow"/>
                    <Label text="오답률 상위 문제" styleClass="quiz-mode-title"/>
                    <Label text="약점을 집중 공략하세요" styleClass="quiz-mode-subtitle"/>
                </VBox>

                <VBox spacing="10" alignment="CENTER_LEFT" prefWidth="360">
                    <HBox alignment="CENTER_LEFT" spacing="8">
                        <Label text="문제 개수 선택" styleClass="quiz-mode-info-label"/>
                        <Pane HBox.hgrow="ALWAYS"/>
                        <Label fx:id="worstCountValueLabel" text="10" styleClass="quiz-mode-info-value"/>
                        <Label text="문제" styleClass="quiz-mode-info-label"/>
                    </HBox>
                    <Slider fx:id="worstCountSlider"
                            min="1" max="50" value="10"
                            blockIncrement="1" majorTickUnit="1"
                            showTickLabels="false" showTickMarks="false"
                            styleClass="quiz-mode-slider"/>
                    <HBox alignment="CENTER" spacing="10">
                        <Button text="5개" styleClass="pill-button quiz-pill-outline" onAction="#onQuickSelect5"/>
                        <Button text="10개" styleClass="pill-button quiz-pill-primary" onAction="#onQuickSelect10"/>
                        <Button text="15개" styleClass="pill-button quiz-pill-outline" onAction="#onQuickSelect15"/>
                        <Button text="20개" styleClass="pill-button quiz-pill-outline" onAction="#onQuickSelect20"/>
                    </HBox>
                </VBox>

                <VBox spacing="8" styleClass="quiz-mode-info yellow">
                    <HBox alignment="CENTER_LEFT" spacing="8">
                        <Label text="선택된 문제" styleClass="quiz-mode-info-label"/>
                        <Pane HBox.hgrow="ALWAYS"/>
                        <Label fx:id="worstSelectedCountLabel" text="10문제" styleClass="quiz-mode-info-value"/>
                    </HBox>
                    <HBox alignment="CENTER_LEFT" spacing="8">
                        <Label text="예상 소요시간" styleClass="quiz-mode-info-label"/>
                        <Pane HBox.hgrow="ALWAYS"/>
                        <Label fx:id="worstTimeLabel" text="15분" styleClass="quiz-mode-info-value"/>
                    </HBox>
                </VBox>

                <Button text="시작하기"
                        styleClass="quiz-mode-start yellow"
                        onAction="#onStartWorst"/>
            </VBox>
        </HBox>
    </center>
</BorderPane>
